import 'package:flutter/material.dart';

class NewPersonaButtons extends StatelessWidget {
  const NewPersonaButtons({
    super.key,
    required this.isValid,
  });

  final bool isValid;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
